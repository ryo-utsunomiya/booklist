<table class="table table-bordered">
    <?php foreach ($book->columnMap() as $column) { ?>
        <tr>
            <th><?= $column ?></th>
            <td><?= h($book->get($column)) ?></td>
        </tr>
    <?php } // endforeach ?>
</table>